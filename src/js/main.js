$(document).ready(function(){
  $('.advantages-slider').slick({
    infinite: true,
    dots: true,
    autoplay: true,
    speed: 500
  });
  $('.customers-slider').slick({
    infinite: true,
    dots: false,
    // autoplay: true,
    speed: 500
  });
});
